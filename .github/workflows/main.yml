name: CI/CD Pipeline #112543312353214
on:
    push:
        branches: [ dev , prod ]
    workflow_dispatch:
permissions:
    contents: write
jobs:
    workflows:
        uses: HishamKoptaN/flutter-devops-core/.github/workflows/00-main.yml@prod
        secrets: inherit