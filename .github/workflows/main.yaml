name: CI/CD Pipeline

on:
    push:
        branches: [ dev, prod ]
    workflow_dispatch:

jobs:
    workflows:
        uses: HishamKoptaN/flutter-devops-core/.github/workflows/main.yaml@prod
        secrets: inherit
