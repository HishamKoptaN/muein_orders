name: Distribute Android
on:
  push:
    branches: [prod, prod]
jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: ${{ github.ref_name }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-java@v4
        with:
          java-version: "17"
          distribution: "temurin"
          cache: "gradle"
      - uses: subosito/flutter-action@v2
        with:
          flutter-version: "3.41.4"
          cache: true
          cache-key: "flutter-:os:-:channel:-:version:-:arch:-:hash"
      - uses: shorebirdtech/setup-shorebird@v1
      - uses: actions/cache@v4
        with:
          path: ~/.shorebird
          key: ${{ runner.os }}-shorebird-${{ hashFiles('**/pubspec.yaml') }}
          restore-keys: |
            ${{ runner.os }}-shorebird-

      - uses: actions/cache@v4
        with:
          path: |
            ~/.pub-cache
            .github/scripts/automation_tool/.dart_tool
          key: ${{ runner.os }}-pub-${{ hashFiles('**/pubspec.lock') }}
          restore-keys: |
            ${{ runner.os }}-pub-

      - uses: actions/cache@v4
        with:
          path: .github/scripts/automation_tool/lib/**/*.g.dart
          key: ${{ runner.os }}-generated-${{ hashFiles('**/*.dart') }}
          restore-keys: |
            ${{ runner.os }}-generated-

      - uses: actions/cache@v4
        with:
          path: |
            build/app
            build/flutter_assets
          key: ${{ runner.os }}-flutter-build-${{ hashFiles('**/pubspec.lock', 'lib/**/*.dart') }}
          restore-keys: |
            ${{ runner.os }}-flutter-build-

      - working-directory: .github/scripts/automation_tool
        run: |
          rm -f lib/**/*.freezed.dart lib/**/*.g.dart
          dart pub get
          dart run build_runner build --delete-conflicting-outputs

      - id: deploy_logic
        working-directory: .
        env:
          SHOREBIRD_TOKEN: ${{ secrets.SHOREBIRD_TOKEN }}
          FLAVOR: ${{ github.ref_name }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          PROJECT_ROOT: ${{ github.workspace }}
        run: |
          dart .github/scripts/automation_tool/bin/main.dart deploy
          echo "=== Deployment Outputs ==="
          echo "is_new_release: ${{ steps.deploy_logic.outputs.is_new_release }}"
          echo "version: ${{ steps.deploy_logic.outputs.version }}"
          echo "apk_path: ${{ steps.deploy_logic.outputs.apk_path }}"

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        id: set_apk_name
        run: |
          if [ "${{ github.ref_name }}" = "prod" ]; then
            echo "name=android-apk" >> $GITHUB_OUTPUT
          else
            echo "name=android-apk_${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: ${{ steps.set_apk_name.outputs.name }}
          path: build/app/outputs/flutter-apk/*.apk
          retention-days: 30

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        uses: wzieba/Firebase-Distribution-Github-Action@v1
        with:
          appId: ${{ secrets.FIREBASE_APP_ID }}
          serviceCredentialsFileContent: ${{ secrets.FIREBASE_ADMIN_SDK }}
          groups: "internal-testers"
          file: build/app/outputs/flutter-apk/app-${{ github.ref_name }}-release.apk

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        id: set_release_tag
        run: |
          if [ "${{ github.ref_name }}" = "prod" ]; then
            echo "tag=latest" >> $GITHUB_OUTPUT
          else
            echo "tag=latest_${{ github.ref_name }}" >> $GITHUB_OUTPUT
          fi
        shell: bash

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.set_release_tag.outputs.tag }}
          name: ${{ github.ref_name == 'prod' && 'Latest Release' || format('Latest {0}', github.ref_name) }}
          files: build/app/outputs/flutter-apk/*.apk
          prerelease: ${{ github.ref_name == 'dev' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add android_appcast.xml
          git commit -m "chore: update appcast [skip ci]"
          git push

      - if: steps.deploy_logic.outputs.is_new_release == 'true'
        working-directory: .github/scripts/automation_tool
        env:
          FIREBASE_PROJECT_NUMBER: "448994699498"
          FIREBASE_APP_ID: ${{ secrets.FIREBASE_APP_ID }}
        run: dart bin/main.dart cleanup
        continue-on-error: true
