# دليل تشغيل الاختبارات في مشروع Mubin Orders

## جدول المحتويات
- [تشغيل جميع الاختبارات](#تشغيل-جميع-الاختبارات)
- [تشغيل اختبارات معينة](#تشغيل-اختبارات-معينة)
- [تشغيل اختبارات تسجيل الدخول](#تشغيل-اختبارات-تسجيل-الدخول)
- [تشغيل اختبار وحدة معين](#تشغيل-اختبار-وحدة-معين)
- [تشغيل اختبار واجهة المستخدم](#تشغيل-اختبار-واجهة-المستخدم)
- [تشغيل اختبار التكامل](#تشغيل-اختبار-التكامل)
- [نصائح وإرشادات](#نصائح-وإرشادات)

## تشغيل جميع الاختبارات
لتشغيل جميع اختبارات المشروع، استخدم الأمر التالي في الطرفية:

```bash
flutter test
```

## تشغيل اختبارات معينة

### تشغيل اختبارات تسجيل الدخول
لتشغيل اختبارات تسجيل الدخول فقط:

```bash
flutter test test/features/auth/sign_in/
```

### تشغيل اختبار وحدة معين
لتشغيل ملف اختبار معين، مثل `sign_in_bloc_test.dart`:

```bash
flutter test test/features/auth/sign_in/sign_in_bloc_test.dart
```

### تشغيل اختبار واجهة المستخدم
لتشغيل اختبارات واجهة المستخدم الخاصة بتسجيل الدخول:

```bash
flutter test test/features/auth/sign_in/sign_in_view_test.dart
```

### تشغيل اختبار التكامل
لتشغيل اختبارات التكامل:

```bash
flutter test integration_test/
```

## خيارات إضافية

### تشغيل مع تغطية الكود
لحساب نسبة تغطية الكود بالاختبارات:

```bash
flutter test --coverage
```

### تشغيل مع تفاصيل إضافية
لعرض تفاصيل أكثر عن الاختبارات:

```bash
flutter test -v
```

### تشغيل مع فلتر محدد
لتشغيل اختبارات تحتوي على كلمة معينة في الاسم:

```bash
flutter test --plain-name "test description"
```

## نصائح وإرشادات

1. **اختبار أثناء التطوير**:
   - استخدم `flutter test --watch` لمشاهدة التغييرات وإعادة تشغيل الاختبارات تلقائيًا

2. **تصحيح الأخطاء**:
   - استخدم `print` داخل الاختبارات لتصحيح الأخطاء
   - استخدم `debugger()` لإيقاف التنفيذ عند نقطة معينة

3. **أفضل الممارسات**:
   - اكتب اختبارات صغيرة ومركزة
   - استخدم أسماء توضيحية للاختبارات
   - تأكد من تغطية جميع السيناريوهات (النجاح، الفشل، الحالات الحدية)

4. **الاختبارات المعتمدة على الوقت**:
   - استخدم `fake_async` للتعامل مع العمليات المتعلقة بالوقت

5. **الاختبارات المعتمدة على الشبكة**:
   - استخدم `mocktail` أو `mockito` لمحاكاة استدعاءات الشبكة
   - تجنب إجراء اتصالات حقيقية بالشبكة في الاختبارات

## استكشاف الأخطاء وإصلاحها

إذا واجهت أي مشاكل في تشغيل الاختبارات:

1. تأكد من تثبيت جميع الحزم:
   ```bash
   flutter pub get
   ```

2. تأكد من أنك في المجلد الصحيح للمشروع

3. تأكد من أنك تستخدم أحدث إصدار من Flutter:
   ```bash
   flutter --version
   flutter upgrade
   ```

4. إذا واجهت مشاكل في الذاكرة:
   ```bash
   flutter test --no-pub --test-randomize-ordering-seed=random
   ```
